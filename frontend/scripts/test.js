import fs from 'fs';
import { createCanvas, loadImage } from 'canvas';

// Since canvas might not be installed, let's write a pure client-side auto-crop helper in imageCompressor.js or ReceiptStudio.jsx
